creator: Admin
default_result_value: ''
description: >
  Generic Torq workflow runner. Posts to the Torq webhook with a JSON
  'Workflow Input' object and optional inclusion of the current alert payload.
  If Torq API credentials are provided, polls the
  Torq Public API for final execution output.
dynamic_results_metadata:
  - result_example_path: resources/run_workflow_json_result_example.json
    result_name: JsonResult
    show_result: true
integration_identifier: Torq
name: Run a Workflow
parameters:
  - name: Workflow Input
    description: JSON object to pass into the Torq workflow
    type: string
    is_mandatory: true
    default_value: '{}'
  - name: Include Alert Payload
    description: If true, include the current alert payload under 'alert'
    type: boolean
    is_mandatory: false
    default_value: false
  - name: Poll Max Seconds
    description: Maximum seconds to poll the execution
    type: string
    is_mandatory: false
    default_value: '60'
  - name: Poll Interval Seconds
    description: Seconds between poll requests
    type: string
    is_mandatory: false
    default_value: '3'
script_result_name: ScriptResult